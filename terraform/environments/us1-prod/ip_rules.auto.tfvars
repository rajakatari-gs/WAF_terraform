# ============================================================
# US1-PROD — IP Sets & Rules
# Add new IP-based WAF rules here.
# PRODUCTION — Changes require PR approval + zero-change plan review.
# ============================================================

ip_sets = {}

ip_set_rules = []
